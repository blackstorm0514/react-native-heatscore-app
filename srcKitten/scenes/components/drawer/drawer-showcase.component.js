import React from 'react';
import { Button } from '@ui-kitten/components';

export const DrawerShowcase = (props) => {
    return (
        <Button
            {...props}>
            TOGGLE DRAWER
        </Button>
    );
};

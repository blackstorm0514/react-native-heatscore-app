const defaultRadioGroup = {
    props: {},
};

const defaultSection = {
    title: 'Default',
    items: [
        defaultRadioGroup,
    ],
};

export const radioGroupShowcase = {
    title: 'Radio Group',
    sections: [
        defaultSection,
    ],
};
